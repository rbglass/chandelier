"use strict";
import assert from "assert";
import helpers from "./helpers";

import jobitems from "../../../api/models/jobitems";

describe("jobitems", () => {
	before(done => helpers.start("job_items", done));
	after(done => helpers.drop(done));

	describe("jobitems - getAll", () => {
	});

	describe("jobitems - create", () => {
	});

	describe("jobitems - update", () => {
	});

	describe("jobitems - delete", () => {
	});
});

