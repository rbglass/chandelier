"use strict";
import sinon from "sinon";
import assert from "assert";
import React from "react/addons";
let { TestUtils } = React.addons;

describe("SingleJobDetails", () => {
	it("renders nodes based on the 'type' field of the row in detailsConfig", () => {});
	it("renders nodes in columns, creating a new column when the config type is 'textarea'", () => {

	});
	it("calls this.props.onBlur with the job id and full details set when focus is lost from a node", () => {

	});
});
